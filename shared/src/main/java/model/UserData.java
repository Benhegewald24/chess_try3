package model;

public record UserData()
{
    static String username;
    static String password;
    static String email;

//    public class registerUser()
//    {
        // print to screen: "Username: "
        // username (initiatized above) = userinput (whatever the user types)
        // print to screen: "Password: "
        // password (initialized above) = userinput (whatever the user types)
        // print to screen: "Email: "
        //email (initialized above) = userinput (whatever the user types)
        // format this data into Json
//    }
}
